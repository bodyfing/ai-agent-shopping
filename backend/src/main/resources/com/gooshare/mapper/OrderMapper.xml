<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gooshare.mapper.OrderMapper">
    <insert id="addOrder">
        INSERT orders(id,user_id, item_id, seller_id, price)
        VALUES (#{orderId},#{userId},#{itemId},#{sellerId},#{price})
    </insert>
</mapper>