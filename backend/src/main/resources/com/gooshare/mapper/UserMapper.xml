<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gooshare.mapper.UserMapper">
    <select id="loginByUsername" resultType="com.gooshare.entity.User">
        SELECT
            id,username,phone,avatar,create_time,role
        FROM user
        WHERE user.username = #{username} AND user.password = #{password}
    </select>

    <select id="getUserByPhone" resultType="com.gooshare.entity.User">
        SELECT
            id,username,phone,avatar,create_time,role
        FROM user
        WHERE phone = #{phone}
    </select>

</mapper>